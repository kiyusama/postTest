<h1>それぞれの投稿</h1>
<p><%= @post.title %></p>
<p><%= @post.body %></p>
<p><%= @post.note %></p>

<%= link_to '編集する', edit_post_path(@post.id) %><br>
<%= button_to '削除', post_path(@post.id), method: :delete, data: { confirm: '本当に削除しますか？' } %><br>
<%= link_to '投稿一覧ページへ', posts_path %>